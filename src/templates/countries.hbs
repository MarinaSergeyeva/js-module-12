{{#each this}}
<li>
    <h2>{{name}}</h2>
    <div>
        <p>Capital: {{capital}}</p>
        <p>Population: {{population}}</p>
        <p>Languages: </p>
        <ul>
            {{#each languages}}
            <li>{{this.name}}</li>
            {{/each}}
        </ul>
    </div>
</li>
{{/each}}